# Microcontroller Development Containers

## Scripts

**Purpose**: Runs the appropriate Docker container based on the provided argument.

**Usage**:

Starts the Zephyr container:

```bash
./docker-run.sh zephyr
```

Starts the GNU Arm container:

```bash
./docker-run.sh gnu-arm
```
